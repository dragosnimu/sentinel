This directory holds `.env.local`, which contains real secrets.

It is gitignored. Nothing in here is ever committed, packaged into a deploy
tarball, or passed on a command line. Secrets reach the server over the
encrypted SSH channel via stdin only.

Create it with:  ./scripts/secrets-init.sh
