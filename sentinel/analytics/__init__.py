"""Aggregation and interpretation.

Counting is easy; saying what the count MEANS is the useful part. This package
turns the raw tables into a small number of statements an operator can act on —
deterministic rules, no model calls, so the dashboard renders instantly and says
the same thing every time.
"""
