"""Threat intel enrichment: CISA KEV (built), FIRST EPSS (later).

These turn a raw CVE into a prioritised finding — is it exploited now (KEV),
how likely soon (EPSS). Mirrored locally so a scan never blocks on a feed.
"""
